global relu, dot, matrix_multiplication

section .text

relu:
    ; ReLU implementation (already translated)

dot:
    ; Dot product implementation (already translated)

matrix_multiplication:
    ; Matrix multiplication implementation (already translated)
